# ── Web delivery (FastAPI + Jinja consent pages) ─────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
Jinja2>=3.1,<4.0
python-multipart>=0.0.9
itsdangerous>=2.1,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0

# ── Model Context Protocol (MCP) ─────────────────────────────────────────────
# Pinned to the version proven against Claude.ai.
fastmcp==3.4.5
