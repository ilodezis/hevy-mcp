# ── Web delivery (FastAPI + Jinja consent pages) ─────────────────────────────
# FastMCP 4 requires Starlette 1.x, supported by FastAPI 0.133+.
fastapi>=0.133,<1.0
uvicorn[standard]>=0.30,<1.0
Jinja2>=3.1,<4.0
python-multipart>=0.0.9
itsdangerous>=2.1,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0

# ── Model Context Protocol (MCP) ─────────────────────────────────────────────
# Pinned and covered by offline legacy + modern MCP integration checks.
fastmcp==4.0.3
